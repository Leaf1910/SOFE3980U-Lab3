apiVersion: apps/v1
kind: Deployment
metadata:
  name: Updated-mysql-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
        - image: Updated-mysql/mysql-server
          name: mysql
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: passwrd
            - name: MYSQL_USER
              value: admin
            - name: MYSQL_PASSWORD
              value: 1234
            - name: MYSQL_DATABASE
              value: Readings
          ports:
            - containerPort: 3307 
              name: mysql
